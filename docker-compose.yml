services:
  # MongoDB Database
  mongodb:
    image: mongo:latest
    container_name: accessishop-db
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  # Backend API
  backend:
    build: ./server
    container_name: accessishop-server
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://fatimah:fatimah123@cluster0.fawev10.mongodb.net/accessishop?retryWrites=true&w=majority
      - JWT_SECRET=your_jwt_secret_here
      - PORT=5000

  # Frontend UI
  frontend:
    build: ./client
    container_name: accessishop-client
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  mongo-data:
